#include<stdio.h>
int main()
{
int a,b,c;
Scanf("%d%d%d",&a,&b,&c);
Printf("enter the number of students in the class:%d",a);
Printf("enter the number of teams:%d",b);
Printf("enter the number of students in each team:%d",c);
{
s=a-(b*c);
Printf("left of students:%d",s);
}
return 0;
}
