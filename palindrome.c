#include <stdio.h>
#include<conio.h>
int main(void)
{
	char a[6],b[6];
	int i;
	a[6]=madam;
	strcpy(b,a);
	strrev(a);
	i==strcmp(a,b);
	if(i==0)
	{
		printf("it is a palindrome");
	}
else
{
 printf("it is not a palindrome");
}
	return 0;
}
